import type { NextPage } from "next";

import styles from "../styles/Home.module.css";

const Landing: NextPage = () => {
  return (
    <div className={styles.container}>
      <div>pages/index.tsx 여기가 랜딩 페이지가 되면 되지 않을까?</div>
    </div>
  );
};

export default Landing;
